<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.catit">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.CatIt"
      tools:ignore="AllowBackup">
    <activity
        android:name=".view.ViewActivity"
        android:theme="@style/Theme.CatIt.NoActionBar" android:exported="true">
    </activity>
    <activity
        android:name=".view.FormActivity"
        android:theme="@style/Theme.CatIt.NoActionBar" android:exported="true">
    </activity>
    <activity
        android:name=".view.HomeActivity"
        android:theme="@style/Theme.CatIt.NoActionBar" android:exported="true">
    </activity>
    <activity
        android:name=".MainActivity"
        android:theme="@style/Theme.CatIt.NoActionBar" android:exported="true"
        tools:ignore="Instantiatable">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <meta-data
        android:name="com.google.android.gms.ads.APPLICATION_ID"
        android:value="ca-app-pub-4655087742659933~6261216122"/>
  </application>
</manifest>